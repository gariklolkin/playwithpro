# Project Context

## Purpose

**PlayWithPro** — a two-sided marketplace connecting amateur table tennis players with verified professional players and coaches for paid one-on-one sessions.

Exactly three service types (keep it simple — anything further the parties arrange themselves):

1. **Video analysis** (`video_analysis`) — amateur uploads footage, pro reviews it live on a video call
2. **Consultation** (`consultation`) — a plain video call
3. **Game** (`game`) — an **in-person** practice game at a physical venue (pro specifies city/club); no video room — the calendar invite carries the venue address, and session completion relies on mutual confirmation + the dispute window

Core value loop: an amateur uploads game footage → picks a verified pro by language/service/price → books an available time slot → pays (funds held in escrow) → both parties meet on a video call → after the session is confirmed, funds are released to the pro minus a platform fee.

## Roles

- **Amateur (player)** — browses catalog, uploads videos, books and pays for sessions, confirms session completion, leaves reviews.
- **Professional (coach)** — creates a profile (bio, achievements, languages, services with per-service hourly prices), submits credentials for verification, publishes availability slots, conducts sessions, receives payouts.
- **Admin** — validates professional profiles, resolves disputes, oversees users and transactions.

## Tech Stack

- **Frontend:** Next.js (App Router) + TypeScript, Tailwind CSS, shadcn/ui, next-intl (locales: en, fr, de, ru, zh; `en` is default)
- **Backend:** NestJS + TypeScript, Prisma ORM, PostgreSQL
- **Monorepo:** pnpm workspaces + Turborepo (`apps/web`, `apps/api`, `packages/*`)
- **Video storage:** S3-compatible (AWS S3 in prod, MinIO locally), pre-signed upload URLs
- **Video calls:** `VideoProvider` abstraction (describe room + issue per-participant token). Decision (2026-09-09, supersedes the 2026-07-20 embedded-Jitsi decision): **pro–amateur sessions run on self-hosted LiveKit** — native call UI in the session-room page (`@livekit/components-react` hooks, our markup, next-intl strings), server-minted short-lived tokens bound to the user and the session room (the room slug grants nothing by itself), attendance connection evidence from signed LiveKit webhooks, one media-server process in dev (compose) and prod (k3s, host network). Synced playback stays on the platform socket.io channel (deliberately not LiveKit's data channel). **Admin verification calls keep Google Meet** (existing flow via Calendar API); Google Meet is never embedded. Join always goes through a platform session-room page so attendance is logged — calendar invites carry the session-room URL, never a direct media-server link.
- **Calendar:** `CalendarProvider` abstraction — Google Calendar API for users who connected Google, universal `.ics` email invites otherwise
- **Payments:** `PaymentProvider` abstraction with escrow semantics (`hold` / `release` / `refund`); mock provider in MVP, real provider (candidate: Stripe Connect) to be decided later
- **Local dev:** Tilt + Docker Compose (postgres, minio, mailpit, api, web)
- **CI:** GitHub Actions (lint, typecheck, test, build)

## Project Conventions

- All project documents, code, comments, commit messages — **English**. Conversation with the project owner is in Russian.
- UI copy externalized in next-intl message catalogs; no hard-coded strings.
- Design language: Notion-inspired (see `design/DESIGN.md`) — ink `#37352F` on white, pastel status tags, emoji icons, 8/12px radii.
- API: REST, OpenAPI-documented via NestJS decorators; DTO validation with class-validator.
- Database: Prisma schema is the source of truth; migrations checked in.
- Money: integer minor units (cents) + ISO 4217 currency code; never floats.
- Time: store UTC, render in viewer's timezone.
- Testing: unit tests colocated; e2e for booking/payment flows are mandatory before a change is archived.
- Observability providers (`Analytics` / `ErrorReporter` / `FeatureFlags`, change 24): business code never imports the vendor SDK — API modules inject the `ANALYTICS` / `ERROR_REPORTER` / `FEATURE_FLAGS` tokens, web code imports only `lib/observability/*` (the sole `posthog-js` import is `lib/observability/client.ts`). No token configured = no-op providers and zero egress (dev, CI). Browser capture is consent-gated (banner, `pwp_consent` cookie); API money/lifecycle events are emitted regardless (see the privacy page).
- Feature flags: a flag is named after the OpenSpec change it guards (e.g. `add-product-observability-support-panel`) and is removed from code when that change is archived; only flags explicitly documented as kill switches may outlive their change. Flags are server-evaluated and bootstrapped to the client (`useFlag(name, default)`); every flag has a code default that applies without a token.
- Session lifecycle (canonical state machine):
  `draft → pending_payment → paid_escrow → in_progress → awaiting_confirmation → completed_paid | disputed → resolved`
  Implementation notes (2026-07-20): `draft` is never persisted (booking config is client-side; `POST /bookings` creates the session directly in `pending_payment` while atomically claiming the slot); a `cancelled` terminal state exists for unpaid/expired bookings (15-minute payment window, config `BOOKING_PAYMENT_TTL_MIN`) and, since change 9, for paid sessions cancelled by either party before slot start (full refund, slot reopens).

## Tech debt / known issues

- In-container `tsc --watch` cold compile takes ~5 min on the owner's machine (Rancher Desktop VM file I/O). Consider: running dev servers as Tilt `local_resource` on the host instead of containers, or SWC builder for NestJS. Revisit when it becomes painful.

## Roadmap (planned changes)

1. `add-project-foundation` ✅ — monorepo scaffold, Tilt dev env, CI, empty web/api apps
2. `add-auth-and-accounts` ✅ — email+password & Google OAuth, roles, sessions (JWT), account settings
3. `add-i18n` ✅ — locale routing, message catalogs for 5 languages, language switcher
4. `add-pro-profiles-verification` ✅ — coach profile CRUD, services & pricing (incl. mapped venue for the in-person game service), admin verification queue; follow-ups also archived: `add-verification-scheduling` ✅ (slot booking, Google Meet, meeting state machine) and `update-email-verification-code` ✅ (6-digit email confirmation codes)
5. `add-availability-scheduling` ✅ — coach weekly availability template, materialized bookable slots, timezone handling, public open-slot listing
5a. `add-player-profiles` ✅ — amateur player profile (level, experience, style, about), account-level avatar via S3 pre-signed uploads (first S3 usage; `StorageModule` reused by video upload), player card for coaches/admins
6. `add-video-upload` ✅ — S3 multipart pre-signed uploads (Uppy), ffprobe validation + conditional H.264 transcode, video library for amateurs, profile videos summary
7. `add-booking-escrow` ✅ — public coach catalog + coach page, booking flow with atomic slot claiming and payment expiry, `PaymentProvider` abstraction + mock with escrow hold, per-session coach access to attached videos, session lists for both roles
8. `add-session-rooms-calendar` ✅ — session room page with an embedded call (originally Jitsi, replaced by LiveKit in change 14; call + video player side by side for video-analysis sessions), `CalendarProvider` with `.ics` email invites pointing at the session-room URL (Google Calendar/Meet implementations deferred until a Google account connection exists), attendance logging, clock-driven `paid_escrow → in_progress → awaiting_confirmation` progression; in-person game sessions get a venue-address invite instead of a video room. v2 candidate spun off from here: `add-synced-playback` (done as change 12)
9. `add-confirmation-payouts-disputes` ✅ — mutual confirmation (player confirm or 48h auto-confirm releases escrow; coach confirm recorded as evidence), `SettlementService` as the single exactly-once caller of `PaymentProvider.release/refund` (sweep retries failures), dispute flow with admin queue + release/refund resolution, pre-start cancellation of paid sessions (full refund, slot reopens, `.ics` CANCEL)
10. `add-reviews-ratings` ✅ — player reviews (1–5 stars + text) gated on paid-out sessions (`completed_paid` or `resolved`+release; status-based, one per session), drift-free aggregate rating (integer sum/count on ProProfile, avg derived) on catalog cards / coach page / coach dashboard, public paginated reviews listing; reviews immutable for users in MVP
11. `add-admin-console` ✅ — admin console area: user directory with account suspension (sign-in/refresh lockout), read-only transaction ledger, analytics overview (per-currency money totals, 30-day trend), review moderation (deletion decrements ProProfile ratingSum/ratingCount in the same transaction); existing verification and dispute queues linked into the console nav
12. `add-synced-playback` ✅ — shared playback of the attached video in video-analysis rooms over a platform WebSocket channel (socket.io `/playback-sync`, party-only, join-window-scoped): play/pause/seek propagate both ways, catch-up on join/reconnect, drift correction, per-user sync toggle, autoplay-block resume overlay; state is ephemeral last-writer-wins (single API instance, Redis adapter is the scale-out path)
13. `add-production-deploy` ✅ — k3s single-node deployment on the netcup VPS (staging today, behind a basic-auth gate): Traefik ingress + cert-manager, in-cluster PostgreSQL with nightly dumps to object storage, Prisma migration Job, deploy scripts from the operator's Mac; Jitsi tasks superseded by change 14; archived 2026-09-10 with the full production smoke, playback-stutter re-test and resource audit still open (owner verifies on staging)
14. `replace-jitsi-with-livekit` ✅ — self-hosted LiveKit replaces the Jitsi iframe: `VideoProvider` mints per-participant tokens, native call UI with pre-join, LiveKit webhooks stamp `connectedAt`/`leftAt` on attendance, one media-server process (compose in dev, host-network Deployment in k3s with embedded TURN); dev call verified over ICE/TCP, deployed to staging 2026-09-09; archived 2026-09-10 with the staging browser matrix, TURN-fallback check and resource audit still open (owner verifies)
15. `add-video-annotations` ✅ — drawing over the attached video in video-analysis rooms: canvas overlay with pen / line / angle (degrees at the vertex) tools, frame-normalized coordinates (letterbox-aware), strokes bound to a 0.1 s moment and shown only while paused there, moment chips that seek both parties, per-role colors, per-author undo, clear; realtime relay over the existing `/playback-sync` channel (`annotation:*` events) with in-memory per-session state, server-side caps, catch-up on (re)connect; ephemeral in MVP — persistence/review/export is the follow-up `add-annotation-review`
16. `add-annotation-review` 📝 proposed 2026-09-10 — persist annotations (`SessionAnnotation`, write-through from the gateway, load on room creation), party-only `GET /sessions/:id/annotations`, read-only review page `/sessions/[id]/review` with moment chips and PNG export (needs storage CORS for GET), annotation activity summary as dispute evidence; prerequisite for the recording replay in 17
17. `add-session-recording` 📝 proposed 2026-09-10 — paid recording add-on (percentage of the service price, platform-owned, refunded at settlement if no recording is delivered via `release(ref, { refundMinor })`), two-sided per-session consent with versioned text and evidence rows (coach profile opt-in gates the offer), lifecycle through `VideoProvider.startRecording/stopRecording` (LiveKit Egress + Redis, S3 output, `egress_*` webhooks, `RECORDING_MAX_CONCURRENT` cap cancels instead of degrading the call), in-room REC indicator + stop, party playback with 90-day retention and player deletion, admin playback only during an open dispute (logged); phase 1 consultation = room composite MP4, phase 2 video analysis = audio-only egress + persisted playback timeline + replay mode on the review page

18. `add-multi-video-attachments` ✅ implemented 2026-09-12, committed `cc05885`, deployed to staging 2026-09-13 (migration applied, backfill verified; owner re-test pending, not archived) — several clips per video-analysis session (a player has separate drills, not only a match recording): `Session.videoId` becomes an ordered `SessionVideo` join with a per-clip note ("forehand loop", "serve"), room + review page switch between clips (playback-sync and annotation state keyed per clip). Attachment limits are technical platform limits and are deliberately NOT tied to the session duration (the parties pause the video to talk, so video minutes ≠ call minutes): per-file limits stay (existing `VIDEO_MAX_SIZE_MB` = 2048 / `VIDEO_MAX_DURATION_MIN` = 30), plus a per-session cap on clip count and on total clip duration/size (e.g. 5 clips, 60 min, 4 GB — config, not per-service); storage cost is bounded separately by a per-account library quota (total GB + video count) with retention for clips not attached to any session. Limits are shown as a meter in the attach step and on the upload page before selection; over-limit attachments are refused with the remaining allowance in the message. Clips are chosen at booking time and can be added/replaced until the session starts within the same limits, no price impact. Phase 2 (only if coaches ask): coach-priced per-extra-clip add-on above the included count, charged into the same escrow hold
19. `add-call-time-reminder` ✅ implemented 2026-09-15, spec synced + archived 2026-09-16 (not committed; dev browser smoke green; staging verification pending) — `SessionRoomResponse.serverNow`, `useSessionClock` (server offset, boundary-aligned ticks, crossing-only thresholds), `RoomToasts` portal into the fullscreen element; in-room time control: persistent remaining-time indicator in the room header derived from `Session.endsAt` (server-time offset, not the client clock), non-blocking toast at T-10 min and at T-0; 10 min is a constant (no user setting in MVP), informational only, the call is not cut off (session progression stays clock-driven per change 8); small enough to ride along with 18 or 20
20. `add-platform-feedback` 💡 idea 2026-09-12 — post-session feedback flow. Player → coach feedback already exists as public reviews (change 10: 1–5 + text, shown on catalog cards and the coach page, gated on paid-out sessions) and stays the only public signal; optional extension is 2–3 sub-ratings (analysis quality, communication, punctuality) shown on the coach page. New: platform feedback — connection quality (1–5), room usability (1–5), free text, optional auto-attached LiveKit connection stats — one per party per session, prompted on room leave and on the session page from `awaiting_confirmation` onwards in the same flow as the review (review step first, platform step second, both skippable); admin-only, surfaced as an analytics view in the console with per-coach / per-period aggregates
21. `update-video-analysis-room-layout` 🛠️ implemented 2026-09-13 (not committed; local browser smoke and staging re-test pending) — Option A "Theatre + presence rail" from the "Session Review Layouts" canvas: after joining, the clip takes the main column (~924px, card width from clip aspect with a 560px floor so portrait footage widens the rail) and the call moves to a 260px rail of equal height (counterpart tile, own tile, controls at the bottom; hide-own-tile remembered per browser; focus mode hides the rail without reconnecting); custom review player bar replaces native controls (frame step by clip fps, scrub with annotated-moment markers, speed menu, local loop, sync toggle, card fullscreen); annotation tools as a left-edge rail; stacked <1000px, single column <640px. `SessionVideoItem` gains `fps/width/height`; no DB change. Archive after 18 and `add-playback-speed`
22. `update-settings-dialog` ✅ implemented, deployed to staging (`ca5f2a0`, no migration) and archived 2026-09-14 (real-phone check of the nested crop dialog still open) — account settings open as a tabbed dialog (Profile: photo/name/language/timezone; Security: password/Google) over the current page instead of a separate route: `?settings=profile|security` on any signed-in page (reload-safe, Back/Escape/backdrop close it, other query params preserved), `SettingsDialogHost` mounted in the locale layout, first Base UI usage via a shadcn-style `components/ui/dialog.tsx`, avatar crop as a nested dialog; `/settings/account` is a locale-preserving redirect to `/dashboard?settings=…` so `next=` returns keep working; user-menu and dashboard-sidebar "Settings" open the dialog in place; the user-menu "Dashboard" item is removed (account-only menu, the logo leads signed-in users to the dashboard). No API/DB change
23. `add-player-context-for-coaches` 🛠️ implemented 2026-09-16 (migration `add_session_goal`; api unit + e2e, web vitest green; browser smoke 5.2 and staging verification pending) — GitHub issue [#1](https://github.com/gariklolkin/playwithpro/issues/1): coaches see the player card (level, style, handedness, grip, goals) only through a shared session from `paid_escrow` onward (card travels in the session data; `GET /players/:id` becomes admin-only), optional per-session goal (≤500 chars, editable by the player until start, shown in checkout), collapsed "About the player" on the coach's bookings entry and in both room headers, player-side visibility hint + "How coaches see you" preview, explicit never-filled state. Modifies `player-profiles`, `booking`, `session-rooms`, `session-video-attachments`. Background: `PlayerCard` exists since change 12 but is rendered nowhere.
24. `add-product-observability` 🛠️ implemented 2026-09-16 (not committed; lint/tsc/tests green with no keys; production verification checklist in `infra/posthog/README.md` pending the operator's project setup) — GitHub issue [#2](https://github.com/gariklolkin/playwithpro/issues/2). Decision: **PostHog Cloud EU (one free-tier project = production; dev/CI send nothing without a token)** for error tracking, product analytics (API emits money/lifecycle events as source of truth), session replay (inputs + sensitive text masked, tokens/pre-signed URLs sanitized), feature flags (server-evaluated + bootstrapped; flag named after its OpenSpec change, removed on archive) and localized in-app support (`posthog.conversations` API with our own UI, HMAC identity from the API; `support@play-with.pro` email channel needs MX/SPF/DKIM work). Thin `Analytics`/`FeatureFlags`/`ErrorReporter` abstractions like the other providers; same-origin ingestion proxy; localized consent banner + privacy page before any capture. Out of scope: logs/metrics/tracing (OpenTelemetry later), surveys, uptime monitoring, removing the basic-auth gate. Modifies `production-deploy`, `i18n`.
25. `add-feedback-board` 🛠️ implemented 2026-09-16 (manifests, secret rendering, backup, web links, runbook; the operator deploy — DNS record, `apply-secrets.sh` with the Fider keys, `deploy.sh`, first-run admin, board setup — and the production checklist 3.2 are pending) — GitHub issue [#3](https://github.com/gariklolkin/playwithpro/issues/3). Decision: **self-hosted Fider (pinned version) at `feedback.play-with.pro`** on the k3s cluster (own `fider` DB + user in the existing Postgres, Brevo SMTP, dedicated Secret, included in `postgres-backup`, small resource limits, letsencrypt-staging first). L0 integration only: one English invite-only board (public at launch), email sign-in codes, Coach/Player + area tags; app links from the user menu ("Suggest an idea") and a coach-dashboard card, hidden unless `NEXT_PUBLIC_FEEDBACK_URL` is set, with `utm_content=<placement>`. Not placed in the room or on error screens (those belong to support, 24). Later options L1–L3 (Google sign-in, PlayWithPro OAuth, in-app proxied form) decided by usage. Modifies `production-deploy`, `i18n`.
26. `add-session-lifecycle-notifications` 🛠️ implemented 2026-09-17 (staging verification + archive pending) — GitHub issue [#5](https://github.com/gariklolkin/playwithpro/issues/5): transactional outbox (`Notification` rows, minute scan + dispatcher, retries with backoff, exactly once per event/recipient), eleven role-specific session emails (paid receipt/new booking with `.ics`, clips changed debounced, T-24h/T-1h reminders, session ended, completed/payout, dispute opened/resolved incl. admin alert, cancelled with CANCEL `.ics`, review received), API-side ICU catalogs ×5 rendered in the recipient's locale/timezone (existing auth/verification/scheduling emails move onto the same layer), Notifications settings tab + signed one-click unsubscribe (RFC 8058 headers), `Session.calendarSequence` + `CalendarProvider.sendUpdate`, daily budget with 80 % warning and optional-mail skipping (Brevo 300/day). Prerequisite for the emails of #4, #6, #8, #9, #10.
27. `add-no-show-protection` 🛠️ implemented 2026-09-18 (migration `add_no_show_protection`; api 439 unit + 105 e2e, web 209 vitest green; dev smoke player/coach/admin en+ru; staging: deploy with `NO_SHOW_AUTO_RESOLVE=false`, two-account check, enable, archive) — GitHub issue [#4](https://github.com/gariklolkin/playwithpro/issues/4): attendance classification for online sessions at join-window close (held / player no-show / coach no-show / no attendance / evidence gap, partial flag), system-opened disputes with kinds, a 48 h coach response window and automatic refund of uncontested no-shows (`NO_SHOW_AUTO_RESOLVE` kill switch), player confirmation always wins (withdraws a system dispute), games gated on the coach's two-option answer (admin dispute after 7 days of silence), dispute reason categories, attendance summary in `SessionResponse`, player/coach/room/admin UI ×5 locales. Emails reuse change 26's kinds.
28. `add-cancellation-policy` 🛠️ implemented 2026-09-18 (migration `add_cancellation_policy`; api 480 unit + 116 e2e, web 222 vitest green; dev smoke checkout/cancel dialog/partly-refunded en+de; staging verification + archive pending) — first half of GitHub issue [#6](https://github.com/gariklolkin/playwithpro/issues/6): platform cancellation policy snapshotted per session (free ≥ 24 h, 50 % until 2 h, nothing after, 30-min late-booking grace), coach cancellation always a full refund and recorded as late inside the window, cancellation record on the session, `PaymentProvider.release(ref, { refundMinor })` + `Payment.refundedMinor`, deferred exactly-once settlement of late cancellations at the original start with a coach/admin fee waiver (optimistic claim on the payment row), admin force-majeure cancel with a required reason, coach late-cancellation count (90 d) + threshold flag and admin alert, public `GET /cancellation-policy`, policy shown under the slot picker, at checkout and in the cancel dialog, ledger shows partly refunded + the cancellation record. Rescheduling follows as 29 `add-session-rescheduling`.
29. `add-session-rescheduling` 🛠️ implemented 2026-09-18 (migration `add_session_rescheduling` incl. a partial unique index; api 516 unit + 123 e2e, web 229 vitest green; dev smoke propose → accept en+ru; staging verification + archive pending) — second half of GitHub issue [#6](https://github.com/gariklolkin/playwithpro/issues/6): mutual reschedule proposals (1–3 of the coach's open slots, same duration, ≥ 2 h ahead, within 30 days), offered slots held with the booking's own `OPEN → BOOKED` claim and released on every exit, one open proposal per session by a partial unique index, accept moves slot/time in one guarded transaction (escrow, snapshots, room, clips, goal untouched), 24 h / start − 2 h expiry with a cleanup sweep, max 2 per session, `.ics` update via `CalendarProvider.sendUpdate` + re-armed reminders, cancellation interplay (tier floor = no loophole; coach-initiated open/declined/expired proposal ⇒ free player cancel), history on the admin ledger row, propose dialog + pending banner on the list entry and in the room, held-slot label in the availability editor.
30. `add-legal-terms-acceptance` 🛠️ implemented 2026-09-18 (migration `add_legal_acceptance`; api 528 unit + 127 e2e, web 268 vitest green; dev smoke register checkbox de + legal pages + footer; staging verification + archive pending; **counsel must replace the placeholder texts and answer the open questions before the basic-auth gate is removed**) — GitHub issue [#7](https://github.com/gariklolkin/playwithpro/issues/7): document registry in `packages/shared` (terms, privacy, imprint, booking-policy, coach-agreement; versions with `material`, draft marker, authoritative locale), Markdown placeholders ×5 locales under `apps/web/content/legal/` with `{{tokens}}` filled from public `GET /platform-facts`, versioned pages `/legal/<doc>[/<version>]`, append-only `LegalAcceptance` rows (registration, Google completion, verification submission, checkout, re-acceptance) with the visitor's locale, `LegalGuard` on booking/paying/verification/availability (`legal_reacceptance_required`), web interstitial + minor-update banner, hourly update-notice email, site footer everywhere (compact in the room), email legal footer, links on upload/dispute/review forms, acceptance history in the admin user detail, CI test for locale completeness.
31. `add-account-deletion-and-export` 🛠️ implemented 2026-09-18, review fixes `7686301`, deployed to staging 2026-09-18 with all migrations through `add_account_data_requests` (owner re-test pending; staging env still lacks `NO_SHOW_AUTO_RESOLVE=false`, `NOTIFY_UNSUBSCRIBE_SECRET`, `POSTHOG_PROJECT_ID`, `fider-env`) (migration `add_account_data_requests`; api 23 new unit + 18 new e2e (7 e2e suites / 145 tests green), web 282 vitest green; dev smoke: blockers → export zip → request → grace screen → cancel, admin-scheduled deletion + request log, catalog hiding, all emails in Mailpit; staging verification, the operator's `apply-backup-lifecycle.sh` run and archive pending) — GitHub issue [#8](https://github.com/gariklolkin/playwithpro/issues/8): deletion = tombstone (`deleted-<id>@invalid`, empty name, `deletedAt`) + erasure hooks (`ACCOUNT_ERASURE_HOOKS` multi-provider: credentials, player/coach profile, availability, verification, videos, avatars prefix, PostHog person when `POSTHOG_PERSONAL_API_KEY` + `POSTHOG_PROJECT_ID` are set), per-step results on `AccountDataRequest` (resumable, retry from the console), blockers (open money/attendance on either side), re-auth (password or emailed code), 14-day grace with `ActiveAccountGuard` (`account_deletion_pending`) and a web grace screen (cancel / export), request-time side effects (other sessions signed out, unpaid bookings cancelled, coach off the catalog, open slots removed, verification call withdrawn), postponement by 7 days on a new blocker, admin-initiated deletion with reason + request log, JSON export zip (dependency-free STORE writer, 24 h cooldown, 7-day TTL, 10-min pre-signed link), five sessionless email kinds, "Former member" rendering, backup lifecycle rule (30 days) + `reapply` script for restores.
