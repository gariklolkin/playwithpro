export enum ProProfileStatus {
  Draft = "draft",
  PendingReview = "pending_review",
  Verified = "verified",
  Rejected = "rejected",
}

export enum VerificationStatus {
  Pending = "pending",
  Approved = "approved",
  Rejected = "rejected",
}
